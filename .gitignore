# Workspaces (état local de chaque projet — jamais versionner)
runs/

# Cible SSH de test (engine/tools/test-target.sh) : clé privée dédiée,
# config Lima générée, alias SSH — jamais versionner.
.test-target/

# Legacy (ancien layout ; supprimé au jalon 1, gardé pour les vieux clones)
.bootstrap-env
.bootstrap-state
.bootstrap.log
.bootstrap.lock
.bootstrap.web.pid
tp-devops-agent-ia/

# Panneau Python (jalon 2) : environnement virtuel, caches, secrets locaux
.venv/
__pycache__/
*.pyc
.pytest_cache/
.env
*.egg-info/

# Secrets Docker locaux montés dans panel/worker (jalon 2, tâches 20-21)
secrets/

# OS
.DS_Store
Thumbs.db

# Editeurs
.vscode/
.idea/

# Sauvegardes sed
*.bak
